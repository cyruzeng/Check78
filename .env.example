# 78长度测量仪 - 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值

# ======================================
# Supabase 配置 (必需)
# ======================================
# 从 Supabase 项目设置中获取
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ======================================
# 应用配置 (必需)
# ======================================
# 管理员后台密码（生产环境必须设置）
ADMIN_PASSWORD=your-secure-admin-password

# 开发环境默认密码（仅用于本地开发）
# 如果未设置 ADMIN_PASSWORD，开发环境中将使用默认值 'admin123'

# ======================================
# 可选配置
# ======================================
# 应用环境
NODE_ENV=development

# 自定义端口（默认 3000）
# PORT=3000

# ======================================
# 安全建议
# ======================================
# 1. 永远不要将真实的环境变量提交到版本控制系统
# 2. 生产环境必须设置强密码（至少8位，包含大小写字母和数字）
# 3. 定期轮换敏感密钥
# 4. 使用 .env.local 存储本地开发配置
# 5. 在 CI/CD 管道中安全地注入环境变量

# ======================================
# 部署说明
# ======================================
# Vercel 部署：
# 1. 在 Vercel 项目设置中添加环境变量
# 2. 确保 ADMIN_PASSWORD 已设置
# 3. 验证 Supabase 连接

# 其他平台：
# 根据平台文档配置相应的环境变量